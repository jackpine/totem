'use strict'

class User{
    constructor(username, email, publicToken, privateToken){
        this.email = email;
        this.username = username;
        this.publicToken = publicToken;
        this.privateToken = privateToken;
    }
}
