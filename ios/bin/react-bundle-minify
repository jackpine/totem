#!/usr/bin/env node

var path = require('path');
var bundle = require('../../node_modules/react-native/local-cli/bundle');

bundle.init(['--minify', '--out', path.join(__dirname, '../main.jsbundle')])
