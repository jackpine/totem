'use strict';

var React = require('react-native');

var {
    StyleSheet,
} = React;

var globalStyles = StyleSheet.create({
    navView: {
        margin: 64,
        marginTop: 80,
    },
});

module.exports = globalStyles;
